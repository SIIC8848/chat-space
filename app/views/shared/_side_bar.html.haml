.header
  .header__name
    .header__lists
      %h3.header__lists__user-name
        = current_user.name
      .header__lists__edit
        -# .header__lists__edit__new
        = link_to new_group_path, class: "header__lists__edit__new" do
          %i.fa.fa-edit
          %span<>
        -# .header__lists__edit__account
        = link_to edit_user_path(current_user), class: "header__lists__edit__account" do
          %i.fa.fa-cog
          %span<>
        
  .groups
    - current_user.groups.each do |group|
      .group
        = link_to group_messages_path(group), class: "group" do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。